buildscript {
    repositories {
        jcenter()
    }
}

apply plugin: 'groovy'

repositories {
    jcenter()
}

dependencies {
    compile gradleApi()
    compile 'com.google.code.gson:gson:2.3'
    compile 'org.apache.velocity:velocity:1.7'
}